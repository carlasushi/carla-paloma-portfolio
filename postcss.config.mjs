/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // Include autoprefixer for better cross-browser support

  },
};

export default config;
